const { add } = require("./add.js");
const { subtract } = require("./subtract.js");
const { multiply } = require("./multiply.js");
const { divide } = require("./divide.js");

console.log("after add 10,5, the Answer is ", add(10, 5));
console.log("afetr subtract 10,5, the Answer is ", subtract(10, 5));
console.log("after multiply 10,5, the Answer is ", multiply(10, 5));
console.log("after divide 10,5, the Answer is ", divide(10, 5));


